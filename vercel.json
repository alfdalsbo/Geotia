{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "b=${VERCEL_GIT_PREVIOUS_SHA}; git cat-file -e \"$b\" 2>/dev/null || b=HEAD^; git diff --quiet \"$b\" HEAD -- . ':(exclude)*.md' ':(exclude)**/*.md' ':(exclude)docs/**' ':(exclude).github/**' ':(exclude)_lokalt/**'",
  "crons": [
    {
      "path": "/api/cron/slowgeo-reveal",
      "schedule": "0 6 * * *"
    }
  ]
}
